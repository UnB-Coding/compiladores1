[pytest]
testpaths = tests

# Cobertura dos arquivos Python de teste (conftest + test_*.py)
addopts =
    --cov=tests
    --cov-report=term-missing
    --cov-report=html:build/coverage_html
    --tb=short
